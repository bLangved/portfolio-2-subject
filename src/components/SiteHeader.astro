<header id="header">
  <div class="banner" role="banner">
    <a class="logo-container" href="/" aria-hidden="true">
      <div class="logo"></div>
    </a>

    <nav id="navbar">
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/projects/">Projects</a></li>
      </ul>
    </nav>

    <div class="hamburger-menu" id="hamburgerMenu">
      <button
        class="menu-icon"
        id="hamburgerIcon"
        aria-label="Mobile navigation"
      >
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
  </div>
</header>

<style lang="scss">
  @import '.././scss/custom-effects.scss';

  header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    height: 3.5em;
    z-index: 999;

    .banner {
      display: flex;
      align-items: center;
      height: 100%;
      background-color: var(--body-color-theme);

      .logo-container {
        margin-left: 1.25em;
        .logo {
          background: var(--banner-logo) no-repeat;
          background-size: contain;
          background-position: 50%;
          height: 1.5em;
          width: 7.5em;
        }
      }
      nav {
        position: fixed;
        top: 3.5em;
        left: 0;
        height: 100%;
        width: 100%;
        max-width: 0;
        overflow: hidden;
        box-shadow: var(--primaryboxShadow);
        background-color: var(--main-color-theme);
        transition: max-width 0.5s ease-in-out;
        ul {
          margin: 0;
          font-family: Tahoma, sans-serif, Verdana;
          li {
            padding: 1.5em 2em;
            font-size: 1.125rem;
            font-weight: bolder;
            text-transform: uppercase;
            border-bottom: 1px solid var(--background-clr1);
          }
        }
      }

      nav.nav-open {
        max-width: 100%;
      }
      @media screen and (min-width: 576px) {
        nav.nav-open {
          max-width: 75%;
        }
      }

      .hamburger-menu {
        margin-left: auto;
        margin-right: 1.25em;

        .menu-icon {
          width: fit-content;
          height: fit-content;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          border: none;
          padding: 0;
          background: transparent;

          .icon-bar {
            display: block;
            width: 2em;
            height: 3px;
            margin: 3px 0;
            background-color: var(--color-theme);
          }
        }
      }
    }
  }

  .icon-bar {
    transition: background-color 0.5s ease-in-out;
  }

  .nav-collapsed .icon-bar {
    transition: all 0.3s ease-in-out;
  }
  .menu-icon.nav-collapsed .icon-bar:nth-child(1) {
    transform: translateY(9px) rotate(45deg);
  }

  .menu-icon.nav-collapsed .icon-bar:nth-child(2) {
    opacity: 0;
  }

  .menu-icon.nav-collapsed .icon-bar:nth-child(3) {
    transform: translateY(-9px) rotate(-45deg);
  }

  @media screen and (min-width: 992px) {
    header {
      position: absolute;

      .banner {
        min-width: 992px;
        max-width: 1200px;
        margin: 0 auto;
        box-shadow: none;

        .logo-container {
          margin-left: 2.5em;
        }

        nav {
          all: initial;
          height: 100%;
          display: grid;
          margin-left: auto;

          ul {
            display: flex;
            align-items: center;
            gap: 2.5em;
            height: 3.5em;
            margin: 0;
            margin-right: 1em;

            li {
              padding: 0.75em 0.5em;
              @include underline-effect;

              &:last-child {
                margin-right: 2em;
              }
            }
          }
        }
      }
      .hamburger-menu {
        display: none;
      }
    }
  }
</style>
